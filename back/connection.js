const config = {
    user: 'cafeteria',
    password: 'cafeteria',
    server: 'localhost\\SQLEXPRESS',
    database: 'cafeteria',
    options: {
        encrypt: true,
        enableArithAbort: true,
        trustServerCertificate: true
    }
};

module.exports = config;